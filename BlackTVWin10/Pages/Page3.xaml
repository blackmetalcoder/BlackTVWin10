<Page
    x:Class="BlackTVWin10.Pages.Page3"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlackTVWin10.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer Padding="{x:Bind Frame.Padding, Mode=OneWay}">
            <StackPanel>
                <TextBlock Text="Om BLACKTV" Style="{StaticResource TitleTextBlockStyle}" DataContext="{x:Bind Frame}" Visibility="{Binding PageTitleVisibility}" />
                <TextBlock Text="BLACKTV är ett informations-system. Läs mer på webben" Style="{StaticResource SubheaderTextBlockStyle}" HorizontalAlignment="Center" />
                <HyperlinkButton Content="Sekrestess" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" NavigateUri="http://blacktv.se/policy.html"/>
                <HyperlinkButton Content="BLACKTV på webben" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" NavigateUri="http://blacktv.se"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
